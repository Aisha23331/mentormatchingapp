const User = require('./User');
const Message = require('./Message');

class Conversation {
    //viewFile()

    //downloadFile()

    addMessage(message) {
        
    }
}

module.exports = Conversation;